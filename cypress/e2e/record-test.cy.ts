/**
 * RECORD YOUR TEST HERE
 * 
 * Steps:
 * 1. Open Cypress: npm run cypress:open
 * 2. Select this file (record-test.cy.ts)
 * 3. Click the red record button (Test Studio)
 * 4. Click through your app to record interactions
 * 5. Cypress will generate code automatically
 * 6. Click "Save test" when done
 */

describe('My Recorded Test', () => {
  it('records my interactions', () => {
    // Your recorded test code will appear here
    // Start by clicking the record button in Cypress Test Runner!
  })
});

it('compare', function() {
  cy.visit('http://localhost:5173/')
  
});
